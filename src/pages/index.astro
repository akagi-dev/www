---
import { defaultLang } from '@i18n/ui';

// Redirect to www portal with default language
const base = import.meta.env.BASE_URL;
const redirectPath = base.endsWith('/') ? `${base}www/${defaultLang}/` : `${base}/www/${defaultLang}/`;
return Astro.redirect(redirectPath);
---
