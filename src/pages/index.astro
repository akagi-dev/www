---
import { defaultLang } from '@i18n/ui';

// Use Astro.site and Astro.url.pathname for proper base path handling
const base = import.meta.env.BASE_URL;
const redirectPath = base.endsWith('/') ? `${base}${defaultLang}/` : `${base}/${defaultLang}/`;
return Astro.redirect(redirectPath);
---
