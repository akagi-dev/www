---
import Layout from '@layouts/Layout.astro';
import { useTranslations } from '@i18n/ui';

const t = useTranslations('ru');
---

<Layout>
  <div class="min-h-screen">
    <!-- Hero Section -->
    <section class="bg-gradient-to-r from-akagi-black to-akagi-gray text-white py-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h1 class="text-4xl md:text-5xl font-bold mb-6 text-akagi-red">
          {t('about.title')}
        </h1>
        <p class="text-xl text-gray-300 max-w-3xl">
          {t('about.description')}
        </p>
      </div>
    </section>

    <!-- Content Sections -->
    <section class="py-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid md:grid-cols-3 gap-8">
          <!-- JDM Specialists -->
          <div class="bg-white p-8 rounded-lg shadow-md hover:shadow-xl transition">
            <div class="text-akagi-red text-4xl mb-4">🏎️</div>
            <h2 class="text-2xl font-bold mb-4">{t('about.jdm.title')}</h2>
            <p class="text-gray-600">
              {t('about.jdm.description')}
            </p>
          </div>

          <!-- Motorsport Excellence -->
          <div class="bg-white p-8 rounded-lg shadow-md hover:shadow-xl transition">
            <div class="text-akagi-red text-4xl mb-4">🏁</div>
            <h2 class="text-2xl font-bold mb-4">{t('about.motorsport.title')}</h2>
            <p class="text-gray-600">
              {t('about.motorsport.description')}
            </p>
          </div>

          <!-- Passion -->
          <div class="bg-white p-8 rounded-lg shadow-md hover:shadow-xl transition">
            <div class="text-akagi-red text-4xl mb-4">❤️</div>
            <h2 class="text-2xl font-bold mb-4">{t('about.passion.title')}</h2>
            <p class="text-gray-600">
              {t('about.passion.description')}
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Image Placeholder -->
    <section class="py-16 bg-gray-100">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="bg-gradient-to-r from-akagi-gray to-akagi-black rounded-lg h-96 flex items-center justify-center">
          <div class="text-center text-white">
            <p class="text-2xl font-bold mb-2">📸 Заполнитель изображения</p>
            <p class="text-gray-400">JDM Дрифт-кар / Мастерская / Автоспорт</p>
          </div>
        </div>
      </div>
    </section>
  </div>
</Layout>
